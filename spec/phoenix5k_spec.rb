require 'spec_helper'

describe Phoenix5k do
  it 'has a version number' do
    expect(Phoenix5k::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
